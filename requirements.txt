# --- Core runtime (training / inference / normalization) ---
torch>=2.4
numpy>=1.24
blosc2>=2.7          # simulator cache (T1DMDataset cache path)

# --- Plotting & report figures ---
matplotlib>=3.7

# --- GUI (gui.py, gui_renderer.py, gui_controls.py) ---
pygame>=2.5

# --- Tests ---
pytest>=7

# --- Real-data cohorts (realdata/) — optional; only for OhioT1DM / Shanghai / AZT1D / UVA-Padova eval ---
pandas>=2.0          # realdata/uvapadova.py
openpyxl>=3.1        # realdata/shanghai.py (.xlsx)
xlrd>=2.0            # realdata/shanghai.py (legacy .xls)
simglucose>=0.2.2    # realdata/uvapadova.py (UVA/Padova sim harness; heavy, optional)
